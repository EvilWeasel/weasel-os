# OpenClaw Agent Rules

- Operate on `weasel-os` through pull requests, not direct pushes.
- Treat system rebuild and secret changes as explicit, auditable actions.
- Do not assume internet-facing services are allowed by default.
