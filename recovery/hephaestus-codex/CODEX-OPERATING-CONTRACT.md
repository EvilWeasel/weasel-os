# Codex operating contract (temporary name pending protected AGENTS.md approval)

Read `README.md` before any action. This file preserves the short operational
boundary while the standard `AGENTS.md` filename is awaiting a separate
protected-instruction-file approval.

- Never access Blain/workplace systems, VPNs, aliases, credentials, files,
  documents, SaaS, or networks.
- Do not read or copy secrets, private keys, browser data, chat/memory stores,
  Discord/Cloudflare credentials, Codex auth/config contents, or unrelated
  personal files.
- Use only `hephaestus-netbird` for approved recovery SSH; do not change SSH,
  NetBird, Tailscale, DNS, firewall, identity, or cloud state.
- Start read-only. For any mutation, state exact effect, risks, and rollback and
  obtain Tobi's explicit approval. Do not restart-flap services.
- Codex never receives a Hetzner token. The proposed iris broker accepts only
  reviewed one-time human-approved fixed actions for server `162472190`; it is
  not deployed by this bundle and does not authorize a Cloud mutation.
- Treat any missing precondition, active Cloud action, lock, health-gate result
  other than 0/20, or failed fixture as indeterminate. Preserve evidence and
  escalate; do not invent recovery authority.
